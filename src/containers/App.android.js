'use strict';

import React, { Component } from 'react';
import MovieApp from '../components/test/MovieApp';

export default class App extends Component {
    render() {
        return (
            <MovieApp />
        );
    }
}
